# Model Adapter 设计（子文档，占位）

本文档将细化模型适配层：统一 chat 接口、结构化动作输出协议、解析与重试策略、以及 MockModel 的实现要求。

## 1. 目标

- 不绑定供应商：可适配 OpenAI-compatible/Anthropic/本地模型
- 强制结构化动作输出，支持鲁棒解析与错误恢复

## 2. 结构化动作协议

- 动作类型与载荷格式
- 输出校验与纠错（缺字段、非法枚举、路径非法等）

## 3. MockModel

- 用例驱动输出动作序列
- 支持 evals 离线回归

